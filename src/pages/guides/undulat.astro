---
import Base from "../../layouts/Base.astro";
import { animals } from "../../data/animals";
const base = import.meta.env.BASE_URL;

const undulat = animals.find((animal) => animal.slug === "undulat");
---

<Base title="Undulat guide – StemmeForDyrene">
    <section class="container py-12">
        <h1 class="font-heading text-3xl mb-6">Undulat guide</h1>
        <div class="grid md:grid-cols-2 gap-6">
            {
                undulat ? (
                    <div class="block bg-white rounded-2xl border border-brand-primary/20 p-5 shadow-soft hover:border-brand-primary">
                        <h2 class="font-heading text-xl">{undulat.name}</h2>
                        <p class="text-sm opacity-80 mt-1">{undulat.short}</p>
                        <div class="mt-4">
                            <h3 class="font-heading text-lg mb-2">
                                Pleje og boligkrav
                            </h3>
                            <p class="text-sm opacity-80">{undulat.care}</p>
                        </div>
                    </div>
                ) : (
                    <p>Information om undulater er ikke tilgængelig lige nu.</p>
                )
            }
        </div>
    </section></Base
>
